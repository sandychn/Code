用哈希O(N)预处理，可以通过枚举字符串的一个端点，在O(N)的时间内求得子串的哈希值，与模式串的哈希值比较即可得出答案。
但是由于哈希值范围的局限性，有可能犯错。所以利用KMP算法可以更准确地得到答案。

在线性时间内判定 A[1~N] 是否为 B[1~M] 的子串

next[i] 表示“A中以i结尾的非前缀子串”与“A的前缀”能够匹配的最长长度
f[i] 表示“B中以i结尾的子串”与“A的前缀”能够匹配的最长长度

2019-07-26